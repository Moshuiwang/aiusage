# Design Tokens

这里放跨端展示一致性规则。

目标：

- 同一个状态在 Web、iPhone、Android、macOS、Windows 上含义一致。
- `ok`、`stale`、`failed`、`missing`、`observed quota`、`estimated` 等状态有统一视觉语义。
- 紧凑入口如 Widget、菜单栏、托盘使用更高信息密度；Web 和 App 可以承载 drilldown。

这里不放平台代码，不放业务聚合逻辑。
